#Os métodos estáticos são métodos que não dependem de um objeto. Tais métodos são chamados sem que haja uma instância
# da classe que declara o método. Como o método estático não possui ligação com um objeto, então o método não pode usar
# variáveis de instância, que são variáveis de um objeto.